name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      with:
        api-level: 34
        build-tools: 34.0.0
        
    - name: Check app structure
      run: |
        echo "📁 Проверяем структуру приложения..."
        find app/ -type f -name "*.xml" -o -name "*.java" | sort
        echo ""
        echo "📋 Содержимое папок:"
        ls -la app/ || echo "Папка app не существует"
        ls -la app/src/ || echo "Папка app/src не существует"
        ls -la app/src/main/ || echo "Папка app/src/main не существует"
      
    - name: Simple APK build attempt
      run: |
        cd app
        echo "🔧 Попытка сборки..."
        
        # Создаем временную папку для сборки
        mkdir -p build/outputs/apk
        
        # Создаем простой текстовый файл вместо APK для теста
        echo "Android APK Build Test" > build/outputs/apk/app-debug.txt
        echo "This would be your APK file" >> build/outputs/apk/app-debug.txt
        echo "Build successful!" >> build/outputs/apk/app-debug.txt
        
        echo "✅ Тестовая сборка завершена"
      
    - name: Upload test artifact
      uses: actions/upload-artifact@v4
      with:
        name: test-build
        path: app/build/outputs/apk/app-debug.txt
        retention-days: 1
